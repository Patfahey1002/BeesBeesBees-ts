class Bee extends Grub {
    job: string;

    constructor(age: number, color: string, food: string, eat: string) {
        super();
        this.age = 5;
        this.color = 'yellow';
        this.job = 'keep on growing';
    }

};